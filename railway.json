






{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "nixpacks",
    "buildCommand": "cd laravel-api && rm -f composer.lock && composer install --no-dev --optimize-autoloader && php artisan storage:link --force"
  },
  "deploy": {
    "startCommand": "cd laravel-api && php artisan serve --host=0.0.0.0 --port=$PORT"
  }
}