{
  "build": {
    "builder": "nixpacks",
    "buildCommand": "cd laravel-api && touch /tmp/database.sqlite && composer install --no-dev --optimize-autoloader && php artisan migrate --force && php artisan config:cache",
    "startCommand": "cd laravel-api && php artisan serve --host=0.0.0.0 --port=$PORT"
  }
}
